import subprocess
import argparse
import os
import sys
import atexit
import time

import datetime
import pandas as pd


if 'SUMO_HOME' in os.environ:
    tools = os.path.join(os.environ['SUMO_HOME'], 'tools')
    sys.path.append(tools)
else:
    sys.exit('please declare environment variable "SUMO_HOME"')

import traci
import sumolib
from sumolib import checkBinary
import numpy as np

lane = 'BA01_0'

for i in range(6):
	sumolib.net.getShape(lane)
	shape_info = sumolib.net.getShape(lane)
	print(shape_info)
	#python generateParkingLots.py -i 'P5' -x 10 -y 0 -b "20,5,35,15" -n 1 -c lane -a 90 -s 70 -e 80

